{
    "name": "enclave",
    "type": "module",
    "scripts": {
        "build": "bun nuxt build --prerender",
        "build:ssr": "bun nuxt build",
        "dev": "bun nuxt dev",
        "tauri:dev": "bun tauri dev",
        "tauri:build": "bun tauri build",
        "generate": "bun nuxt generate",
        "preview": "bun nuxt preview",
        "postinstall": "bun nuxt prepare"
    },
    "dependencies": {
        "@nuxt/icon": "^1.15.0",
        "nuxt": "^4.0.0",
        "vue": "^3.5.17",
        "vue-router": "^4.5.1"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.6.2"
    }
}
